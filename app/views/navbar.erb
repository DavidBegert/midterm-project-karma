<nav id="header-navbar" class="navbar navbar-inverse navbar-fixed-top navbar-transparent">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <button class="btn btn-link navbar-btn navbar-theme <%= ' navbar-active' if request.path_info == "/" %>" onclick="location.href='/';">Home</button>
        <% if current_user %>
          <button class="btn btn-link navbar-btn navbar-theme <%= ' navbar-active' if current_user && request.path_info == "/users/#{current_user.id}" %>" onclick="location.href='/users/<%= current_user.id %>';"><%= current_user.username %> (<%= user_karma_tally(current_user) %>)
          </button>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <button class="btn btn-link navbar-btn navbar-theme" id="logged" onclick="location.href='/users/logout';">Logout</button>
        <% else %>
          <li>
            <div class="dropdown navbar-btn navbar-theme">
              <button class="btn btn-link dropdown-toggle navbar-theme" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                Login
                <span class="caret"></span>
              </button>
              <ul class="dropdown-menu navbar-inverse navbar-transparent" aria-labelledby="dropdownMenu1">
                <%= get_login_dropdown_user_list %>
              </ul>
            </div>
          </li>
          <button class="btn btn-link navbar-btn navbar-theme <%= ' navbar-active' if request.path_info == "/users/new" %>" onclick="location.href='/users/new';">Sign Up</button>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
